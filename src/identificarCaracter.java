import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Scanner;

public class identificarCaracter {
    public static void main(String[] args) {
        /*
        //identificar carácter

        Scanner sc = new Scanner(System.in);
        List<String> lista = new ArrayList<String>();
        String[] vocales = {"a","i","o","u"};
        lista.addAll(Arrays.asList(vocales));

        char caracter = sc.next().charAt(0);
        boolean caso1=Character.isLetter(caracter);
        boolean caso2 =Character.isDigit(caracter);
        boolean caso3=Character.isWhitespace(caracter);
        List<Boolean> lista1= new ArrayList<Boolean>();
        lista1.add(caso1);
        lista1.add(caso2);
        lista1.add(caso3);

        boolean resultado= false;
        for (boolean variable:lista1) {
            if(variable==true){
                resultado=true;
            }
        }
        switch (resultado){
            case true: {
                if (Character.isLetter(caracter)) {
                    if (lista.contains(caracter)) {
                        System.out.println("es una vocal");
                    } else {
                        System.out.println("es una consonante");
                    }
                }

                if (Character.isDigit(caracter)) {
                    System.out.println("es un digito");
                }

                if (Character.isWhitespace(caracter)) {
                    System.out.println("es un whitespace");
                }

            }
            default:{
                System.out.println("es un caracter especial");
            }


        }
    */
    }


}
